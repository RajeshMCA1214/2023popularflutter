class ModelItem{
  //String? image;
  String? name;
  String? productName;
  String? pending;
  String? completed;
  double? price;
  int? quantity;

  ModelItem( this.name, this.productName, this.pending, this.completed, this.price,
      this.quantity);
}